#ifndef OGLE_SF_WAV_H
#define OGLE_SF_WAV_H

#include <OpenAL/al.h>

namespace sf {

class WavFx {
public:
  WavFx(const char *file);
  ~WavFx();

  ALuint get() const;

private:
  ALuint buffer;
};
}

#endif // OGLE_SF_WAV_H
